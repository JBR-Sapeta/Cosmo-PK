import { type ReactElement } from 'react';
import { NarrowPageLayout } from '@Layout/View';

export default function ContactPage(): ReactElement {
  return <NarrowPageLayout>Contact Page</NarrowPageLayout>;
}
