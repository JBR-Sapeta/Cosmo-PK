import { type ReactElement } from 'react';
import { NarrowPageLayout } from '@Layout/Base';

export default function EditorPage(): ReactElement {
  return <NarrowPageLayout>Editor Page</NarrowPageLayout>;
}
