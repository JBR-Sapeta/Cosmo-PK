import { type ReactElement } from 'react';
import { NarrowPageLayout } from '@Layout/Base';

export default function ProfilePage(): ReactElement {
  return <NarrowPageLayout>Profile Page</NarrowPageLayout>;
}
